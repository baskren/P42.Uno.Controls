<Page
    x:Class="P42.Uno.Controls.Test.PageMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:P42.Uno.Controls.Test"
    xmlns:system="using:System"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid.Children>
            <TextBlock Text="Test Pages" HorizontalAlignment="Stretch" HorizontalTextAlignment="Center" VerticalAlignment="Center"/>
            <ListView x:Name="_listView" Grid.Row="1">
                <ListView.ItemTemplate >
                    <DataTemplate x:DataType="system:Type"  >
                        <TextBlock Text="{x:Bind Name}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid.Children>
    </Grid>
</Page>
